# coding: utf-8

"""
    Asterwise API

    Vedic astrology, numerology, horoscope, and PDF report API. Send birth data, get accurate sidereal calculations as structured JSON.  **Base URL:** https://api.asterwise.com  **Authentication:** Bearer token — `Authorization: Bearer YOUR_API_KEY`  Get your free API key at https://asterwise.com

    The version of the OpenAPI document: 2026-03-01
    Contact: support@asterwise.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class BrandingParams(BaseModel):
    """
    BrandingParams
    """ # noqa: E501
    brand_name: Optional[Annotated[str, Field(strict=True, max_length=100)]] = Field(default='', description="Developer's brand/company name shown on cover and header.")
    brand_logo_url: Optional[Annotated[str, Field(strict=True, max_length=500)]] = Field(default='', description="URL to brand logo image (PNG/SVG). Rendered in PDF header.")
    brand_primary_color: Optional[Annotated[str, Field(strict=True)]] = Field(default='#1a1a2e', description="Hex color for headings and accents. Default: #1a1a2e (dark navy).")
    brand_tagline: Optional[Annotated[str, Field(strict=True, max_length=200)]] = Field(default='', description="Optional one-liner shown under brand name on cover.")
    prepared_by: Optional[Annotated[str, Field(strict=True, max_length=100)]] = Field(default='', description="Astrologer or consultant name shown on cover page.")
    footer_text: Optional[Annotated[str, Field(strict=True, max_length=300)]] = Field(default='', description="Custom footer text. Example: 'yoursite.com | contact@yoursite.com'")
    cover_background_color: Optional[Annotated[str, Field(strict=True)]] = Field(default='#1a1a2e', description="Hex color for cover page background. Default: #1a1a2e.")
    __properties: ClassVar[List[str]] = ["brand_name", "brand_logo_url", "brand_primary_color", "brand_tagline", "prepared_by", "footer_text", "cover_background_color"]

    @field_validator('brand_primary_color')
    def brand_primary_color_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^#[0-9A-Fa-f]{6}$", value):
            raise ValueError(r"must validate the regular expression /^#[0-9A-Fa-f]{6}$/")
        return value

    @field_validator('cover_background_color')
    def cover_background_color_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^#[0-9A-Fa-f]{6}$", value):
            raise ValueError(r"must validate the regular expression /^#[0-9A-Fa-f]{6}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BrandingParams from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BrandingParams from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "brand_name": obj.get("brand_name") if obj.get("brand_name") is not None else '',
            "brand_logo_url": obj.get("brand_logo_url") if obj.get("brand_logo_url") is not None else '',
            "brand_primary_color": obj.get("brand_primary_color") if obj.get("brand_primary_color") is not None else '#1a1a2e',
            "brand_tagline": obj.get("brand_tagline") if obj.get("brand_tagline") is not None else '',
            "prepared_by": obj.get("prepared_by") if obj.get("prepared_by") is not None else '',
            "footer_text": obj.get("footer_text") if obj.get("footer_text") is not None else '',
            "cover_background_color": obj.get("cover_background_color") if obj.get("cover_background_color") is not None else '#1a1a2e'
        })
        return _obj


